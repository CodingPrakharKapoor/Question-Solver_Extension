// This script will be executed when the answer window is opened.
document.addEventListener('DOMContentLoaded', () => {
    const solutionDiv = document.getElementById('solution');
    // The content will be set by the content script when the window is opened.
  });
  